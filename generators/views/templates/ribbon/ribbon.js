/*@ngInject*/
module.exports = function RibbonCtrl() {
  var ctrl = this;

  _.defaults(ctrl, {
    title: "<%= titlename %>"
  });
  // ========================================
  // interface
  // ========================================

  // ========================================
  // implementation
  // ========================================
};
